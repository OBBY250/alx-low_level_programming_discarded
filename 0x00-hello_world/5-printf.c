#include <stdio.h>
/**
 * main - this will be used by gcc -wall option
 * Return: 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
